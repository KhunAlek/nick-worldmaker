(function(){
  "use strict";
  // Shared backend mode: Mission 1, Mission 2, and all lock states are rendered by assets/js/app.js.
  // The former local later-mission renderer is intentionally disabled so it cannot overwrite live shared progress.
})();
