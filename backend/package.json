{
  "name": "nick-worldmaker-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply worldmaker-db --local",
    "db:remote": "wrangler d1 migrations apply worldmaker-db --remote",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@cfworker/json-schema": "^4.1.1"
  },
  "devDependencies": {
    "wrangler": "^4.24.3"
  }
}
